enum Environment {
  TEST = 'test',
  DEV = 'development',
  PRODUCTION = 'production',
}

enum EnvironmentFilesName {
  TEST = '.env.test',
  DEV = '.env',
}

export { Environment, EnvironmentFilesName }